{
  "name": "stack-and-borrow",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.4",
    "@vitejs/plugin-react": "4.3.4",
    "vite": "^6.1.0",
    "wrangler": "^4.90.1"
  }
}
